<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jobplatform.fo.enterprise.domain.mapper.ResumeMapper">
	<!-- 이력서 리스트 데이터 갯수 구해오기 -->
	<select id="loadResumeListCount" resultType="int">
		select count(1)
		from P2_TBL_RESUME_M
		where mbr_sq = #{mbr_sq}
			and use_yn = 'Y'
	</select>
	
	<!-- 이력서 리스트 데이터 얻기 -->
	<select id="selectResumeData" resultType="jobplatform.fo.enterprise.domain.vo.ResumeListVO">
		select rsm_sq, mbr_sq, rsm_rprsntv_yn, rsm_tl, updt_dtm
		from P2_TBL_RESUME_M
		where mbr_sq = #{searchData.mbr_sq}
		order by updt_dtm ${searchData.sort}
		limit #{paginationData.startRowIndex},#{paginationData.showDataCountPerPage};
	</select>
	
</mapper>